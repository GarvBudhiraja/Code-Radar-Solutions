#include<stdio.h>
#include<string.h>
int main(){
    int alpha;
    scanf("%d",&alpha);
    printf("Hexadecimal: %d", alpha);
    int num;
    scanf("%d", &num);
    printf("Octal: %d", num);
    return 0;
}